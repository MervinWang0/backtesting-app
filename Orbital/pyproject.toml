[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "backtesting-app"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["base*", "Orbital*"]
